github:
  protected_branches:
    main:
      required_status_checks:
        # contexts are the names of checks that must pass
        contexts:
          - build (pull_request)
          - dependency-submission (pull_request)
          - dependency-review (pull_request)
          - Dependency Review / dependency-review (pull_request)
          - dependency-review
          - Dependency Review
